// Placeholder. El template default de Flutter no aplica al admin_web,
// que monta su shell solo despues de un login JWT real. Los smoke tests
// de la app viven en scripts/smoke.ps1 y scripts/smoke-deep.ps1.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
