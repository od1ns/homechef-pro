// Non-web fallback: nothing to do (the desktop/iOS/Android admin would
// hand the bytes off to a file picker or share sheet — out of scope here).
void downloadBytes(List<int> bytes, String filename, String contentType) {
  // no-op
}
